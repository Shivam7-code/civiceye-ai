# Copy this file to ".env" (note the leading dot) and paste your real key.
# No VITE_ prefix: the key is read server-side by the Vite dev proxy
# (vite.config.js) and is never exposed to the browser bundle.
GROQ_API_KEY=gsk_your_real_groq_key_here
